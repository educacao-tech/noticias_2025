{
  "name": "noticias_2025",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "predev": "npx kill-port 3001 && npx kill-port 5500",
    "dev": "cross-env OPEN_BROWSER_ON_START=true nodemon server.js --ignore news.json --ignore .browser_lock --ext js,json,html,css"
  },
  "devDependencies": {
    "cross-env": "^10.1.0",
    "nodemon": "^3.0.0"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2"
  }
}
